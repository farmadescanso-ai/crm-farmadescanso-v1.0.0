<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/colors.css" rel="stylesheet">
    <link href="/css/dashboard.css" rel="stylesheet">
    <style>
        .table-container {
            max-height: 75vh;
            overflow: auto;
            border: 1px solid #dee2e6;
            border-radius: 0.75rem;
        }
        .table th {
            position: sticky;
            top: 0;
            background-color: #fff;
            z-index: 10;
            border-bottom: 2px solid #dee2e6;
        }
        .table td {
            padding: 0.75rem;
            vertical-align: middle;
        }
        .cooperativa-row:hover {
            background-color: rgba(11, 137, 27, 0.05);
        }
    </style>

    <%- include('../partials/navbar-styles') %></head>
<body>
    <%- include('../partials/navbar-dashboard') %>

    <div class="container-fluid mt-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div>
                        <h1 class="mb-0 text-primary"><i class="fas fa-handshake me-1 text-primary"></i>Cooperativas</h1>
                        <p class="text-muted mb-0">Gestiona las cooperativas farmacéuticas</p>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <span class="badge bg-primary text-white fs-6">Total: <%= cooperativas.length %></span>
                        <a href="/dashboard/cooperativas/nuevo" class="btn btn-success">
                            <i class="fas fa-plus me-1"></i>Nueva Cooperativa
                        </a>
                    </div>
                </div>

                <% if (query && query.success === 'cooperativa_creada') { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i>Cooperativa creada correctamente.
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
                <% if (query && query.success === 'cooperativa_actualizada') { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i>Cooperativa actualizada correctamente.
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
                <% if (query && query.success === 'cooperativa_eliminada') { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i>Cooperativa eliminada correctamente.
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>
                <% if (error || (query && query.error)) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <%= error || 'No se pudo completar la acción solicitada.' %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% } %>

                <div class="table-container">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>ID</th>
                                <th>Nombre</th>
                                <th>Email</th>
                                <th>Teléfono</th>
                                <th>Contacto</th>
                                <th class="text-center">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (cooperativas.length === 0) { %>
                            <tr>
                                <td colspan="6" class="text-center text-muted py-4">
                                    <i class="fas fa-inbox fa-2x mb-2"></i><br>
                                    No hay cooperativas registradas
                                </td>
                            </tr>
                            <% } else { %>
                            <% cooperativas.forEach(function(cooperativa) { %>
                            <tr class="cooperativa-row">
                                <td><%= cooperativa.id || cooperativa.Id %></td>
                                <td><strong><%= cooperativa.Nombre || cooperativa.nombre %></strong></td>
                                <td><%= cooperativa.Email || cooperativa.email %></td>
                                <td><%= cooperativa.Telefono || cooperativa.telefono || '-' %></td>
                                <td><%= cooperativa.Contacto || cooperativa.contacto || '-' %></td>
                                <td class="text-center">
                                    <div class="btn-group" role="group">
                                        <a class="btn btn-sm btn-outline-primary" title="Ver" href="/dashboard/cooperativas/<%= cooperativa.id || cooperativa.Id %>">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a class="btn btn-sm btn-outline-warning" title="Editar" href="/dashboard/cooperativas/<%= cooperativa.id || cooperativa.Id %>/editar">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <form action="/dashboard/cooperativas/<%= cooperativa.id || cooperativa.Id %>/eliminar" method="POST" class="d-inline" onsubmit="return confirm('¿Estás seguro de eliminar esta cooperativa?');">
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Eliminar">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js">    </script>
    <script src="/js/go-to-top.js"></script>

    <!-- Go to Top Button -->
    <button id="goToTop" class="go-to-top" title="Ir arriba">
        <i class="fas fa-arrow-up"></i>
    </button>
</body>
</html>

