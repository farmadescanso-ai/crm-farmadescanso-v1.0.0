<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/colors.css" rel="stylesheet">
    <link href="/css/dashboard.css" rel="stylesheet">
    <style>
        .table-wrap {
            max-height: 75vh;
            overflow: auto;
            border: 1px solid #dee2e6;
            border-radius: 0.75rem;
        }
        .tabla-matriz th {
            position: sticky;
            top: 0;
            background: #fff;
            z-index: 10;
            border-bottom: 2px solid #dee2e6;
            white-space: nowrap;
        }
        .tabla-matriz td {
            white-space: nowrap;
        }
        .price-cell {
            text-align: right;
            min-width: 120px;
        }
        .sticky-col {
            position: sticky;
            left: 0;
            background: #fff;
            z-index: 11;
        }
        .sticky-col-2 {
            position: sticky;
            left: 90px;
            background: #fff;
            z-index: 11;
        }
        .sticky-col-3 {
            position: sticky;
            left: 240px;
            background: #fff;
            z-index: 11;
        }
    </style>
    <%- include('../partials/navbar-styles') %>
</head>
<body>
    <%- include('../partials/navbar-dashboard') %>

    <div class="container-fluid py-4">
        <div class="d-flex justify-content-between align-items-center mb-3 px-3">
            <div>
                <h3 class="mb-0 text-primary"><i class="fas fa-table me-2"></i>Tarifas activas por Artículo</h3>
                <div class="text-muted small">Una columna por cada tarifa activa (solo lectura). Para editar, entra en cada tarifa.</div>
            </div>
            <div class="d-flex gap-2">
                <a class="btn btn-outline-primary" href="/dashboard/ajustes/tarifas-clientes">
                    <i class="fas fa-tags me-1"></i>Volver a Tarifas
                </a>
            </div>
        </div>

        <div class="px-3">
            <% if (error) { %>
            <div class="alert alert-danger"><i class="fas fa-exclamation-triangle me-2"></i><%= error %></div>
            <% } %>
            <% if (success) { %>
            <div class="alert alert-success"><i class="fas fa-check-circle me-2"></i><%= success %></div>
            <% } %>

            <div class="card shadow-sm mb-3">
                <div class="card-body">
                    <form method="GET" class="row g-2 align-items-end">
                        <div class="col-md-4">
                            <label class="form-label fw-semibold">Filtrar por Marca</label>
                            <select class="form-select" name="marcaId">
                                <option value="">— Todas —</option>
                                <% if (marcas && marcas.length > 0) { %>
                                    <% marcas.forEach(function(m) { %>
                                        <option value="<%= m.id %>" <%= String(marcaId) === String(m.id) ? 'selected' : '' %>><%= m.Nombre %></option>
                                    <% }); %>
                                <% } %>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <button class="btn btn-primary" type="submit"><i class="fas fa-filter me-1"></i>Aplicar</button>
                            <a class="btn btn-outline-secondary" href="/dashboard/ajustes/tarifas-clientes/matriz"><i class="fas fa-eraser me-1"></i>Limpiar</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <div class="text-muted">
                            Artículos: <strong><%= articulos ? articulos.length : 0 %></strong> ·
                            Tarifas activas: <strong><%= tarifasActivas ? tarifasActivas.length : 0 %></strong>
                        </div>
                    </div>

                    <div class="table-wrap">
                        <table class="table table-sm tabla-matriz align-middle mb-0">
                            <thead>
                                <tr>
                                    <th class="sticky-col" style="width:90px;">Id</th>
                                    <th class="sticky-col-2" style="width:150px;">SKU</th>
                                    <th class="sticky-col-3" style="width:360px;">Artículo</th>
                                    <% (tarifasActivas || []).forEach(function(t) { %>
                                        <th class="text-end">
                                            <a href="/dashboard/ajustes/tarifas-clientes/<%= t.Id %>/precios" class="text-decoration-none">
                                                <%= t.NombreTarifa %>
                                            </a>
                                        </th>
                                    <% }); %>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (articulos && articulos.length > 0) { %>
                                    <% articulos.forEach(function(a) { %>
                                        <% const articuloId = a.Id || a.id; %>
                                        <tr>
                                            <td class="sticky-col"><%= articuloId %></td>
                                            <td class="sticky-col-2"><%= a.SKUArticulo || a.SKU || '-' %></td>
                                            <td class="sticky-col-3">
                                                <div class="fw-semibold"><%= a.NombreArticulo || a.Nombre || a.nombre || '-' %></div>
                                                <div class="text-muted small"><%= a.MarcaArticulo || a.Marca || '-' %></div>
                                            </td>
                                            <% (tarifasActivas || []).forEach(function(t) { %>
                                                <% const key = `${Number(t.Id)}:${Number(articuloId)}`; %>
                                                <% const precio = preciosMap && preciosMap.get ? preciosMap.get(key) : null; %>
                                                <td class="price-cell">
                                                    <% if (precio !== null && precio !== undefined && !Number.isNaN(Number(precio))) { %>
                                                        € <%= Number(precio).toFixed(2) %>
                                                    <% } else { %>
                                                        <span class="text-muted">—</span>
                                                    <% } %>
                                                </td>
                                            <% }); %>
                                        </tr>
                                    <% }); %>
                                <% } else { %>
                                    <tr><td colspan="<%= 3 + (tarifasActivas ? tarifasActivas.length : 0) %>" class="text-muted">No hay artículos para mostrar.</td></tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/go-to-top.js"></script>
</body>
</html>

