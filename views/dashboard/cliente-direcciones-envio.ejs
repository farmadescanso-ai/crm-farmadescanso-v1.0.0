<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link href="/css/colors.css" rel="stylesheet">
  <link href="/css/dashboard.css" rel="stylesheet">
  <%- include('../partials/navbar-styles') %>
</head>
<body>
  <%- include('../partials/navbar-dashboard') %>
  <div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <div>
        <h3 class="mb-1"><i class="fas fa-truck me-2"></i>Direcciones de envío</h3>
        <div class="text-muted">
          Cliente #<%= cliente.Id || cliente.id %> · <strong><%= cliente.Nombre_Razon_Social || cliente.Nombre || cliente.nombre || '—' %></strong>
        </div>
      </div>
      <div class="d-flex gap-2">
        <a href="/dashboard/clientes/<%= cliente.Id || cliente.id %>" class="btn btn-secondary">
          <i class="fas fa-arrow-left me-1"></i>Volver al cliente
        </a>
        <a href="/dashboard/clientes/<%= cliente.Id || cliente.id %>/direcciones-envio/nueva" class="btn btn-primary">
          <i class="fas fa-plus me-1"></i>Nueva dirección
        </a>
      </div>
    </div>

    <% if (query && query.success) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <i class="fas fa-check-circle me-2"></i>
        <% if (query.success === 'creada') { %>Dirección creada correctamente.<% } %>
        <% if (query.success === 'actualizada') { %>Dirección actualizada correctamente.<% } %>
        <% if (query.success === 'desactivada') { %>Dirección desactivada correctamente.<% } %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <% if (query && query.error) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <i class="fas fa-exclamation-triangle me-2"></i>
        No se pudo completar la operación.
        <% if (query.details) { %>
          <div class="mt-2 small text-muted" style="white-space: pre-wrap;"><%= query.details %></div>
        <% } %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>

    <div class="card shadow-sm">
      <div class="card-body">
        <% if (!direcciones || direcciones.length === 0) { %>
          <div class="text-muted">
            Este cliente aún no tiene direcciones de envío. Puedes crear una nueva.
          </div>
        <% } else { %>
          <div class="table-responsive">
            <table class="table table-hover align-middle">
              <thead>
                <tr>
                  <th>Alias</th>
                  <th>Dirección</th>
                  <th>Contacto</th>
                  <th>Estado</th>
                  <th class="text-end">Acciones</th>
                </tr>
              </thead>
              <tbody>
                <% direcciones.forEach(d => { %>
                  <tr>
                    <td>
                      <div class="fw-semibold"><%= d.Alias || '—' %></div>
                      <% if (Number(d.Es_Principal) === 1) { %>
                        <span class="badge bg-success">Principal</span>
                      <% } %>
                    </td>
                    <td>
                      <div class="fw-semibold"><%= d.Nombre_Destinatario || '—' %></div>
                      <div class="text-muted">
                        <%= d.Direccion || '—' %><% if (d.Direccion2) { %>, <%= d.Direccion2 %><% } %><br>
                        <%= d.CodigoPostal || '—' %> · <%= d.Poblacion || '—' %><% if (d.Pais) { %> · <%= d.Pais %><% } %>
                      </div>
                      <% if (d.Observaciones) { %>
                        <div class="text-muted small mt-1"><i class="fas fa-comment-dots me-1"></i><%= d.Observaciones %></div>
                      <% } %>
                    </td>
                    <td>
                      <div class="fw-semibold">
                        <%= (d.Contacto_Apellidos || d.Contacto_Nombre) ? `${d.Contacto_Apellidos || ''} ${d.Contacto_Nombre || ''}`.trim() : '—' %>
                      </div>
                      <div class="text-muted small">
                        <% if (d.Telefono) { %><div><i class="fas fa-phone me-1"></i><%= d.Telefono %></div><% } %>
                        <% if (d.Email) { %><div><i class="fas fa-envelope me-1"></i><%= d.Email %></div><% } %>
                      </div>
                    </td>
                    <td>
                      <% if (Number(d.Activa) === 1) { %>
                        <span class="badge bg-primary">Activa</span>
                      <% } else { %>
                        <span class="badge bg-secondary">Inactiva</span>
                      <% } %>
                    </td>
                    <td class="text-end">
                      <a class="btn btn-sm btn-outline-warning" href="/dashboard/clientes/<%= cliente.Id || cliente.id %>/direcciones-envio/<%= d.id %>/editar">
                        <i class="fas fa-edit me-1"></i>Editar
                      </a>
                      <% if (Number(d.Activa) === 1) { %>
                        <form class="d-inline" method="POST" action="/dashboard/clientes/<%= cliente.Id || cliente.id %>/direcciones-envio/<%= d.id %>/desactivar" onsubmit="return confirm('¿Desactivar esta dirección?');">
                          <button type="submit" class="btn btn-sm btn-outline-danger">
                            <i class="fas fa-ban me-1"></i>Desactivar
                          </button>
                        </form>
                      <% } %>
                    </td>
                  </tr>
                <% }) %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/go-to-top.js"></script>
  <button id="goToTop" class="go-to-top" title="Ir arriba">
    <i class="fas fa-arrow-up"></i>
  </button>
</body>
</html>

