<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/colors.css" rel="stylesheet">
    <link href="/css/dashboard.css" rel="stylesheet">

    <%- include('../partials/navbar-styles') %></head>
<body>
    <%- include('../partials/navbar-dashboard') %>

    <div class="container py-4">
        <% if (query && query.success === 'relacion_creada') { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>Relación creada correctamente.
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        <% } %>
        <% if (query && query.success === 'relacion_actualizada') { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>Relación actualizada correctamente.
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        <% } %>
        <% if (query && query.error) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-triangle me-2"></i>
            <%= query.error %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        <% } %>

        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">
                            <i class="fas fa-link me-2"></i>Relación #<%= clienteCooperativa.id %>
                        </h5>
                        <div class="btn-group">
                            <a href="/dashboard/clientes-cooperativas/<%= clienteCooperativa.id %>/editar" class="btn btn-light btn-sm">
                                <i class="fas fa-edit me-1"></i>Editar
                            </a>
                            <form action="/dashboard/clientes-cooperativas/<%= clienteCooperativa.id %>/eliminar" method="POST" class="d-inline" onsubmit="return confirm('¿Estás seguro de eliminar esta relación?');">
                                <button type="submit" class="btn btn-danger btn-sm">
                                    <i class="fas fa-trash me-1"></i>Eliminar
                                </button>
                            </form>
                        </div>
                    </div>
                    <div class="card-body">
                        <dl class="row">
                            <dt class="col-sm-4">ID:</dt>
                            <dd class="col-sm-8"><%= clienteCooperativa.id %></dd>

                            <dt class="col-sm-4">Cliente:</dt>
                            <dd class="col-sm-8">
                                <strong><%= clienteCooperativa.ClienteNombre || 'Cliente #' + clienteCooperativa.Id_Cliente %></strong>
                                <br>
                                <small class="text-muted">ID: <%= clienteCooperativa.Id_Cliente %></small>
                                <br>
                                <a href="/dashboard/clientes/<%= clienteCooperativa.Id_Cliente %>" class="btn btn-sm btn-outline-primary mt-1">
                                    <i class="fas fa-external-link-alt me-1"></i>Ver Cliente
                                </a>
                            </dd>

                            <dt class="col-sm-4">Cooperativa:</dt>
                            <dd class="col-sm-8">
                                <strong><%= clienteCooperativa.CooperativaNombre || 'Cooperativa #' + clienteCooperativa.Id_Cooperativa %></strong>
                                <br>
                                <small class="text-muted">ID: <%= clienteCooperativa.Id_Cooperativa %></small>
                                <br>
                                <a href="/dashboard/cooperativas/<%= clienteCooperativa.Id_Cooperativa %>" class="btn btn-sm btn-outline-primary mt-1">
                                    <i class="fas fa-external-link-alt me-1"></i>Ver Cooperativa
                                </a>
                            </dd>

                            <dt class="col-sm-4">Número de Asociado:</dt>
                            <dd class="col-sm-8">
                                <%= clienteCooperativa.NumAsociado || clienteCooperativa.numAsociado || '-' %>
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js">    </script>
    <script src="/js/go-to-top.js"></script>

    <!-- Go to Top Button -->
    <button id="goToTop" class="go-to-top" title="Ir arriba">
        <i class="fas fa-arrow-up"></i>
    </button>
</body>
</html>

