<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/colors.css" rel="stylesheet">
    <link href="/css/dashboard.css" rel="stylesheet">

    <%- include('../partials/navbar-styles') %></head>
<body>
    <%- include('../partials/navbar-dashboard') %>

    <div class="container py-4">
        <% if (query && query.success) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>Operación realizada correctamente.
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        <% } %>

        <div class="row justify-content-center">
            <div class="col-lg-9">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="fas fa-users me-2"></i>Grupo #<%= grupo.id || grupo.Id %></h5>
                        <div class="btn-group">
                            <a href="/dashboard/grupos-compras/<%= grupo.id || grupo.Id %>/editar?returnTo=<%= encodeURIComponent('/dashboard/grupos-compras/' + (grupo.id || grupo.Id)) %>" class="btn btn-light btn-sm"><i class="fas fa-edit me-1"></i>Editar</a>
                            <form action="/dashboard/grupos-compras/<%= grupo.id || grupo.Id %>/eliminar" method="POST" class="d-inline" onsubmit="return confirm('¿Eliminar este grupo?');">
                                <button type="submit" class="btn btn-danger btn-sm"><i class="fas fa-trash me-1"></i>Eliminar</button>
                            </form>
                        </div>
                    </div>
                    <div class="card-body">
                        <dl class="row">
                            <dt class="col-sm-3">ID:</dt>
                            <dd class="col-sm-9"><%= grupo.id || grupo.Id %></dd>
                            <dt class="col-sm-3">Nombre:</dt>
                            <dd class="col-sm-9"><strong><%= grupo.Nombre || '' %></strong></dd>
                            <dt class="col-sm-3">CIF:</dt>
                            <dd class="col-sm-9"><%= grupo.CIF || '—' %></dd>
                            <dt class="col-sm-3">Email:</dt>
                            <dd class="col-sm-9"><%= grupo.Email || '—' %></dd>
                            <dt class="col-sm-3">Teléfono:</dt>
                            <dd class="col-sm-9"><%= grupo.Telefono || '—' %></dd>
                            <dt class="col-sm-3">Contacto:</dt>
                            <dd class="col-sm-9"><%= grupo.Contacto || '—' %></dd>
                            <dt class="col-sm-3">Dirección:</dt>
                            <dd class="col-sm-9"><%= grupo.Direccion || '—' %></dd>
                            <dt class="col-sm-3">Población:</dt>
                            <dd class="col-sm-9"><%= grupo.Poblacion || '—' %></dd>
                            <dt class="col-sm-3">Código Postal:</dt>
                            <dd class="col-sm-9"><%= grupo.CodigoPostal || '—' %></dd>
                            <dt class="col-sm-3">Provincia:</dt>
                            <dd class="col-sm-9"><%= grupo.Provincia || '—' %></dd>
                            <dt class="col-sm-3">País:</dt>
                            <dd class="col-sm-9"><%= grupo.Pais || '—' %></dd>
                            <dt class="col-sm-3">Activo:</dt>
                            <dd class="col-sm-9"><%= (grupo.Activo === 1 || grupo.Activo === true || String(grupo.Activo) === '1') ? 'Sí' : 'No' %></dd>
                            <dt class="col-sm-3">Observaciones:</dt>
                            <dd class="col-sm-9"><%= grupo.Observaciones || '—' %></dd>
                        </dl>
                        <div class="d-flex justify-content-end">
                            <a href="/dashboard/grupos-compras" class="btn btn-secondary">Volver</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/go-to-top.js"></script>
    <button id="goToTop" class="go-to-top" title="Ir arriba"><i class="fas fa-arrow-up"></i></button>
</body>
</html>

