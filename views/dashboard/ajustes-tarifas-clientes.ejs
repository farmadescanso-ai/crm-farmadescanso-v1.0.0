<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/colors.css" rel="stylesheet">
    <link href="/css/dashboard.css" rel="stylesheet">
    <%- include('../partials/navbar-styles') %>
</head>
<body>
    <%- include('../partials/navbar-dashboard') %>

    <div class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h3 class="mb-0 text-primary"><i class="fas fa-tags me-2"></i>Tarifas de Clientes</h3>
            <a class="btn btn-outline-secondary" href="/dashboard"><i class="fas fa-arrow-left me-1"></i>Volver</a>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><i class="fas fa-exclamation-triangle me-2"></i><%= error %></div>
        <% } %>
        <% if (typeof success !== 'undefined' && success) { %>
        <div class="alert alert-success"><i class="fas fa-check-circle me-2"></i><%= success %></div>
        <% } %>

        <div class="row g-3">
            <div class="col-lg-5">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title mb-3">Crear nueva tarifa</h5>
                        <form method="POST" action="/dashboard/ajustes/tarifas-clientes">
                            <div class="mb-3">
                                <label class="form-label fw-semibold">NombreTarifa</label>
                                <input type="text" name="NombreTarifa" class="form-control" placeholder="Ej. Farmacias Premium" required>
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <label class="form-label fw-semibold">Activa</label>
                                    <select name="Activa" class="form-select">
                                        <option value="1" selected>Sí</option>
                                        <option value="0">No</option>
                                    </select>
                                </div>
                                <div class="col-md-6">
                                    <label class="form-label fw-semibold">FechaInicio</label>
                                    <input type="date" name="FechaInicio" class="form-control">
                                </div>
                                <div class="col-md-12">
                                    <label class="form-label fw-semibold">FechaFin</label>
                                    <input type="date" name="FechaFin" class="form-control">
                                    <small class="text-muted">Si se establece FechaFin, la tarifa se considerará inactiva y se aplicará General.</small>
                                </div>
                                <div class="col-md-12">
                                    <label class="form-label fw-semibold">Observaciones</label>
                                    <textarea name="Observaciones" class="form-control" rows="3" placeholder="Notas internas..."></textarea>
                                </div>
                            </div>
                            <div class="mt-3 d-flex gap-2">
                                <button class="btn btn-primary" type="submit"><i class="fas fa-plus me-1"></i>Crear</button>
                                <a class="btn btn-outline-primary" href="/dashboard/ajustes/tarifas-clientes/0"><i class="fas fa-star me-1"></i>Ver General</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-lg-7">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title mb-3">Listado de tarifas</h5>
                        <div class="table-responsive">
                            <table class="table table-sm align-middle">
                                <thead>
                                    <tr>
                                        <th style="width: 70px;">Id</th>
                                        <th>Nombre</th>
                                        <th style="width: 90px;">Activa</th>
                                        <th style="width: 140px;">FechaFin</th>
                                        <th style="width: 220px;"></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (tarifas && tarifas.length > 0) { %>
                                        <% tarifas.forEach(function(t) { %>
                                            <tr>
                                                <td><%= t.Id %></td>
                                                <td><%= t.NombreTarifa %></td>
                                                <td>
                                                    <% if (Number(t.Activa) === 1) { %>
                                                        <span class="badge bg-success">Sí</span>
                                                    <% } else { %>
                                                        <span class="badge bg-secondary">No</span>
                                                    <% } %>
                                                </td>
                                                <td><%= t.FechaFin ? String(t.FechaFin).slice(0,10) : '-' %></td>
                                                <td class="text-end">
                                                    <a class="btn btn-sm btn-outline-primary" href="/dashboard/ajustes/tarifas-clientes/<%= t.Id %>">
                                                        <i class="fas fa-edit me-1"></i>Editar
                                                    </a>
                                                    <a class="btn btn-sm btn-outline-success" href="/dashboard/ajustes/tarifas-clientes/<%= t.Id %>/precios">
                                                        <i class="fas fa-euro-sign me-1"></i>Precios
                                                    </a>
                                                </td>
                                            </tr>
                                        <% }); %>
                                    <% } else { %>
                                        <tr><td colspan="5" class="text-muted">No hay tarifas.</td></tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                        <small class="text-muted">Nota: la tarifa <strong>General (Id=0)</strong> es la base por defecto.</small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/go-to-top.js"></script>
</body>
</html>

